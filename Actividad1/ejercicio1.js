/* Ejercicio 1
  En la consola del navegador, crear un pequeño programa que le pida al
  usuario que ingrese un número, multiplica ese número por 2 y muestra 
  en salida el resultado de esa operación. Guarda el código en un archivo js.
*/

const number = prompt("Por favor introduza un número");
const multiplication = Number(number) * 2;
alert("El resultado de la multiplicación es: " + multiplication);
